<body class="bg-light">

  <!-- Main Section -->
  <main class="ai-section container text-center mt-5 pt-5">
    <h2 class="fw-bold mb-3 ai-title">
      🧠 AI Waste Identifier
    </h2>
    <p class="text-muted mb-4">
      Upload or capture an image and let our AI detect the waste category.
    </p>

    <div class="ai-card mx-auto shadow-lg bg-white p-4 rounded-4">
      <input 
        type="file" 
        accept="image/*" 
        capture="camera" 
        id="imageInput" 
        class="form-control mb-3"
      >
      <button 
        id="identifyBtn" 
        class="btn identify-btn px-4 py-2 fw-semibold rounded-pill"
      >
        Identify Waste
      </button>
      <div 
        id="resultMessage" 
        class="mt-4 fw-semibold fs-5 text-success"
      >
        📷 Upload an image to get started.
      </div>
    </div>
  </main>

  <script src="/js/ai.js"></script>
</body>
